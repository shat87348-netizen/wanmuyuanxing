# 业务功能矩阵：技术要求 ↔ 前端覆盖

> 把《数据处理子系统技术要求》中**与前端直接相关**的条款逐条列出，
> 标注后端是否已实现、前端是否已覆盖、新设计需新增什么。
>
> 本系统是**后台处理任务状态查看与配置管理**界面，非值班指控。

---

## 4.1 多源数据采集模块（前端职责：状态可视化 + 接口配置）

| 条款 | 后端 | 前端现状 | 新设计 |
|---|---|---|---|
| 4.1(6).a ≥ 50 接口接入 | ✅ AgentRegistry 支持任意数 | 部分 data-collection 页 | **采集代理 & 接口** 页（多 agent × 多 instance） |
| 4.1(6).b 接口热插拔 | ⚠️ agent 端有 PluginManager，后台动态注册待 P5 | ❌ | 接口卡上加"启用 / 禁用 / 重启"按钮（仅 admin） |
| 4.1(6).d 统一收集接口状态/流量/概要 | ✅ HeartbeatReport.InstanceStatus | ❌ | 接口实例卡：状态徽章 + 流量曲线 + 异常计数 |
| 4.1(6).e 接入控制 + 速率控制 | ✅ FlowController | ❌ | 接口卡：限速滑块 + 启用开关（仅 admin） |
| 4.1(6).f 异常接口安全处置 | ✅ FlowController.block | ❌ | 接口卡：熔断按钮 + 熔断事件 timeline（仅 admin） |
| 4.1(6).g 插件可独立 C 端 / 嵌入 | ✅ agent-standalone + agent-embedded | ❌ | agent 详情：显示 deployForm |
| 4.1(9) 本地循环存储 | ✅ LocalBuffer | ❌ | agent 详情：显示缓冲占用率 |

## 4.2 数据处理模块（前端职责：参数解析配置 + 监控）

| 条款 | 后端 | 前端现状 | 新设计 |
|---|---|---|---|
| 4.2(1).b 去重 / 合并 / 时间对齐 | ✅ DedupWindow + TimeAligner | ❌ | 系统状态页：显示去重 / 对齐统计 |
| 4.2(1).d 配置文件驱动解析 | ✅ TelemetryConfigLoader | ❌ | **解析模型管理**页：4 tab 编辑器 |
| 4.2(1).e ≥ 30 解析类型 / ≥ 300 万条·秒 | ✅ FormulaEngine + Disruptor | ❌ | 系统状态页：实时吞吐计 |
| 4.2(1).f 多通道一致性融合 | ✅ Fuser | ❌ | 解析模型管理：融合策略下拉 |
| 4.2(1).g 处理模型导入导出 | ✅ ParseModelService（ZIP） | ❌ | **解析模型管理**页：导入/导出 ZIP 按钮（仅 admin 可导入） |
| 4.2(2).a 协议配置 | ✅ TelemetryConfigLoader | 部分 protocol-config 页 | 合并进解析模型管理 |
| 4.2(2).c 物理量公式解析 | ✅ FormulaEngine | ❌ | 公式编辑器（Aviator 表达式） |
| 4.2(2).d 状态阈值判定 | ✅ StateRange + AlarmEngine | ❌ | 阈值规则编辑器 |
| 4.2(2).e 多级预警 | ✅ AlarmLevel 5 级 | 部分 alarm 页 | **告警中心**：按级别筛选 + 颜色区分（不闪烁 / 不响铃） |

## 4.3 数据校准模块

| 条款 | 后端 | 前端现状 | 新设计 |
|---|---|---|---|
| 4.3(1) 野值剔除 | ✅ 4 Cleaner（阈值/3σ/肖维涅/脚本） | 部分 calibration 页 | **数据校准**页：算法选择 + 历史窗口可视化（仅 admin 可配） |

## 4.4 可视化要求（前端核心条款）

| 条款 | 后端 | 前端现状 | 新设计页面 |
|---|---|---|---|
| 4.4(1) 接口统一管理（注册/流量监控） | ✅ AgentRegistry | ❌ | **采集代理 & 接口** |
| 4.4(2) 综合显示卫星/系统/设备数据状态 | ✅ 部分 | Dashboard 总览页 | **总览** |
| 4.4(3) 多元显示（表格 + 曲线） | ✅ | 部分 data-viz 页 | **遥测查询** + **总览** |
| 4.4(4) 实时载荷图像 | ❌ P5 | ❌ | 不在本期范围（占位即可） |
| 4.4(5) 用户自主配置监视信息 | ⚠️ ParseModelPackage.visLayoutJson 已留位 | ❌ | 总览页支持拖拽布局 + 保存（仅 admin） |
| 4.4(6) 可视化配置导入导出 | ✅ ParseModelService ZIP | ❌ | 解析模型管理：导入/导出按钮 |
| 4.4(7) 异常数据报警提示 | ✅ AlarmEngine + WebSocket | 部分 alarm 页 | **告警中心** + header 铃铛红点（**不闪烁不响铃**） |
| 4.4(8) 与"数据交互子系统"UI 集成 | ⚠️ 仅留接口 | ❌ | 长期目标，本期不实现 |

## 5.6 日志记录

| 条款 | 后端 | 前端现状 | 新设计 |
|---|---|---|---|
| 5.6(1) 系统异常日志 | ⚠️ 后端写入 logback，业务库 P5 | log 页 | **日志中心**（轻改） |
| 5.6(2) 采集端口异常日志 | ⚠️ agent.event.* 主题，business P5 | ❌ | 日志中心加 source 过滤 |
| 5.6(3) 同步发送到外部日志服务器 | ⚠️ P5 | ❌ | 系统配置页：syslog 转发开关（仅 admin） |

## 5.7 系统架构

| 条款 | 后端 | 前端现状 | 新设计 |
|---|---|---|---|
| 5.7(1) B/S 架构 | ✅ | ✅ | — |
| 5.7(2) 按试验任务隔离 | ✅ Task | ❌ | **顶部任务选择器** + 全局 X-Task-Id 注入 |
| 5.7(2) 处理模型迁移 | ✅ ParseModel ZIP | ❌ | 解析模型管理：复制到其他任务 |
| 5.7(4) RBAC 模型 | ⚠️ 开发期 admin 单账号 | 部分 user 页 | **2 角色（admin / user）**，前端按角色显隐写操作；后端 P5 接入 |
| 5.7(5) 自启动/自重启 | ✅ K8s | ❌ | 系统状态页：显示 pod 健康 |

---

## 页面 ↔ 技术要求 映射（新设计）

| 新页面 | 覆盖条款 |
|---|---|
| **总览** (overview) | 4.4(2)、4.4(3)、4.4(5)、4.4(7) |
| **告警中心** (alarm) | 4.2(2)e、4.4(7) |
| **采集代理 & 接口** (agent-registry) | 4.1(6)abdef、4.4(1) |
| **试验任务** (task) | 5.7(2) |
| **解析模型管理** (parse-model) | 4.2(1)dg、4.2(2)acd、4.4(6)、5.7(2) |
| **数据校准** (calibration) | 4.3(1) |
| **遥测查询/回放** (telemetry) | 4.4(3) — P5 |
| **日志中心** (log) | 5.6 全部 |
| **系统状态** (system-status) | 4.2(1)be、5.7(5) |

---

## 验收标准（每页设计完后核对）

- [ ] 至少覆盖一条上表条款（标 4.X.Y）
- [ ] 6 主题切换无样式破裂
- [ ] 空 / 加载 / 失败 / 弱网四态
- [ ] 关键操作 ≤ 3 次点击可达
- [ ] PC 1920 与 1366 都可用
- [ ] 色彩对比度 WCAG AA
- [ ] **写操作按角色显隐**（admin 可见 / user 不渲染）
